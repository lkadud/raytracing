# Rust Raytracer

Raytracer written in [Rust](https://rust-lang.org/), following the [Ray Tracing in One Weekend](https://raytracing.github.io/books/RayTracingInOneWeekend.html) book-series.

Currently, chapter one of three is implemented.

Run with: `cargo run --release > image.ppm`
